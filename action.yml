name: 'Generate AI docs'
description: 'Generate AI doc'

inputs:
  input_one:
    description: 'input_one'
    
runs:
  using: "composite"
  steps:
      - run: echo "hello"
        shell: bash
      - run: echo "${{inputs.input_one}}"
        shell: bash
      - run: echo "$CUSTOM_REPO_ENV"
        shell: bash
      - run: echo "$KNOWL_BACKEND_HTTP"
        shell: bash
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: echo "$GITHUB_CONTEXT"
        shell: bash
